type Token @entity {
  id: ID!
  tokenID: BigInt!
  updatedAtTimestamp: BigInt
  owner: User!
  name: String
  description: String
  image: String
  ipfsURI: String
}

type User @entity {
  id: ID!
  tokens: [Token!]! @derivedFrom(field: "owner")
}
